import React from 'react';

const UserCheck = (props) => {
    const {email} = props.ou;
    const omail = props.omail;
    return (
        <div>
            
        </div>
    );
};

export default UserCheck;